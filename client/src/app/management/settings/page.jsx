import React from 'react';
import SiteManager from '@/features/admin/pages/SiteManager';

export const metadata = {
  title: 'Site Builder - KSubZone Control',
  robots: 'noindex, nofollow',
};

export default function Page() {
  return <SiteManager />;
}
