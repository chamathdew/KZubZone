import React from 'react';
import UserManager from '@/features/admin/pages/UserManager';

export const metadata = {
  title: 'User Manager - KSubZone Control',
  robots: 'noindex, nofollow',
};

export default function Page() {
  return <UserManager />;
}
